# Simple tool to start multiple dotnet api
It searches for all directories ending with `.Api` and runs `dotnet run`.

# Usage
```
--ignore   a comma seperated list of api project to skip
--watch    a single api to watch
```
